{
    "version": 2,
    "framework": null,
    "outputDirectory": "public",
    "routes": [
        { "src": "/build/(.*)", "dest": "/build/$1" },
        { "src": "/storage/(.*)", "dest": "/storage/$1" }
    ]
}
